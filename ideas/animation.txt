FxGraph & SSListViews
================================================================================

new FxGraph(A, B, C, D, E);

Animations are a linked list.

closed  lite  more  full
A   ->  B ->  C ->  D -> 


--------------------------------------------------------------------------------
Scenario
--------------------------------------------------------------------------------

User holds down key.

Animation A starts.

Animation.start()

User presses up key.

Animation.


--------------------------------------------------------------------------------
Design
--------------------------------------------------------------------------------

var A = FxNode({
  name: "A",
  Fx: aFx,
  onEnter: function() {},
  onLeave: function() {},
  triggers: {[{type:'keydown', key:'shift'}, 'next']
  	     [{type:'keyup', key:'shift'], 'prev']}
});

var B = FxNode({
  name: "B",
  Fx: bFx,
  onEnter: function() {},
  onLeave: function() {},
  triggers: {[{type:'keyhold, key:'shift', duration:1000}, 'next'],
  	     [{type:'keyup', key:'shift'}, 'A']}
});

var C = FxNode({
  name: "C",
  Fx: bFx,
  onEnter: function() {},
  onLeave: function() {},
  triggers: {[{type:'keyhold, key:'shift', duration:1000}, 'next'],
  	     [{type:'keyup', key:'shift'}, 'A']}
});

var D = FxNode({
  name: "D",
  Fx: cFx,
  onEnter: function() {},
  onLeave: function() {},
  triggers: {[{type:'keyhold, key:'shift', duration:1000}, 'next']
  	     }
});

var E = FxNode({
  name: "E",
  onEnter: Console.open.bind(Console)
});

animGraph.start()
animGraph.go("A")


--------------------------------------------------------------------------------
Thoughts
--------------------------------------------------------------------------------

FxGraphs can also be nodes
