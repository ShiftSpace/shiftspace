// ==Builder==
// @uiclass
// @customView
// @optional
// @package           ShiftSpaceUI
// @dependencies      SSView
// ==/Builder==

var PublishPane = new Class({

  Extends: SSView,
  name: "PublishPane",

  initialize: function(el, options)
  {
    this.parent(el, options);
    SSLog('new PublishPane', SSLogForce);
  }

});