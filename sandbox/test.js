/*
window.addEvent('domready', function() {
  GM_addStyle('body { font: 11px verdana; }');
  GM_log(GM_getValue('test', 'asdf'));
  GM_xmlhttpRequest({
    'method': 'get',
    'url': 'http://google.com/',
    'onload': function(r) {
      GM_log(r.responseText.substr(0, 100));
    }
  });
});
*/